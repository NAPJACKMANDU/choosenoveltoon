import RouterSetting from "./routes/routerList"
import { BrowserRouter } from 'react-router-dom';

function App() {
  return (
      <BrowserRouter>
        <RouterSetting/>
      </BrowserRouter>
  );
}

export default App
